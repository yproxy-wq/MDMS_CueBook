# Cuebook UI Design Rationale (UI設計の根拠)

本ドキュメントは、CuebookのUIレイアウトにおける「なぜそこにあるか」を定義する。
変更を行う際は、本ドキュメントの根拠を覆す論理的な理由が必要である。

## 1. 全体レイアウト (General Layout)
- **Dark Noir Theme**: GMの認知負荷を下げ、セッションの没入感を高めるために採用。明るい色は「警告」や「アクティブな要素」にのみ使用する。
- **2-Column Layout (Default)**: 左側に「進行（Phase）」、右側に「演出（Script/Sound）」を配置。これは人間の視線が「状況把握（左）」から「アクション（右）」へ流れる性質を利用している。

## 2. ヘッダー (Header)
- **Global Volume (左上)**: 最も重要な「音」の制御を、左上の起点に配置。緊急時に迷わずアクセスできる。
- **Mode Switch (右上)**: 「Architect（構築）」と「Session（本番）」の切り替え。本番中に誤操作しないよう、端に配置。

## 3. サイドバー (Phase Sidebar)
- **Vertical Timeline**: 章の進行を上から下へ配置。時間の流れと一致させる。
- **Timer & Warning**: フェーズ名の直下に配置。GMが「今、何分押しているか」を常に視界の端で捉えられるようにするため。

## 4. スクリプトビューワー (Script Viewer)
- **Character Color Coding**: 名前を読む前に「色」で誰のセリフか判断させる。多忙なGMの認知負荷を劇的に下げる。
- **Minimalist Margin**: 無意味な空白を削り、一度に表示できる台本の量を最大化。スクロールの手間を減らす。

## 5. サウンドボード (Sound Board)
- **Active Glow**: 再生中の音源が「光る」演出。どの音が鳴っているかを直感的に把握させる。
- **Choke Groups**: 同時に鳴らせない音（BGM等）を自動で制御し、GMの「音量調整」というタスクを自動化する。

## 6. キャラクター管理 (Dramatis Personae)
- **Token Counter**: プレイヤーの資源（チップ等）をリアルタイムに管理。GMが「誰がどれだけ持っているか」を覚える必要をなくす。
