# CueBook 修正指示書（独立再評価済・最終版）

作成日: 2026-08-02  
対象: Firebase Hosting への配備を予定する CueBook
改訂: 2026-08-02（独立チェックリストによる再評価済）

## 前提と検証方法

現行サーバーで動作していることを前提に、ローカルで依存関係・静的解析・ユニットテスト・本番ビルドを確認した。`git` メタデータがこの作業ディレクトリにないため、変更差分の履歴比較はできない。

依頼された「記憶をすべて消す」は実行できない。代替として、初版の結論を根拠にせず、(1) 秘密情報のクライアント露出、(2) 未認証アクセス、(3) Firebase Hosting の rewrite／header の評価順、(4) 定周期の root state 更新、(5) クリーン依存インストールと配備ゲート、という独立チェックリストで再評価した。本書はその再評価を反映した最終版である。

本書の「実施」は、既存データと共有画面への後方互換性を維持する範囲で行う。Sync Studio／Firestore 共有方式を変更する **CB-029** は、プロジェクト規約上のイミュータブル・コアに当たるため、所有者の明示承認を得たうえで v1.09 に実施した。旧共有 URL は安全上の理由で後方互換にしない。

## 修正対象

### CB-020: ロックファイルと依存関係の不整合

- **検出内容**: `npm ci` が `package.json` と `package-lock.json` の不一致で失敗した。
- **修正指示**: 現在の `package.json` を正とし、lockfile を再生成する。以後は CI で `npm ci` を必ず実行する。
- **注意点**: lockfile を手で編集しない。依存更新時は `package.json` と lockfile を同じ変更としてレビューする。
- **再発防止**: プルリクエストまたは配備前ゲートに `npm ci && npm run verify` を追加する。

### CB-021: 型検査が配備ゲートに含まれていない

- **検出内容**: `vite build` とテストは通るが、`tsc --noEmit` は失敗し、存在しない props、未定義型、データモデルの齟齬を検出した。
- **修正指示**: `typecheck` と、それを含む `verify` スクリプトを追加する。Firebase Hosting に配備する前に `verify` を必須実行する。
- **注意点**: Vite は標準では型検査を行わない。ビルド成功を型安全の証明として扱わない。
- **再発防止**: CI の必須ジョブを `lint`、`typecheck`、`test`、`build` の順に固定する。

### CB-022: ルートコンポーネントの毎秒再描画

- **検出内容**: `App.tsx` が `useAppTimer` の毎秒 state 更新を購読し、アプリ全体を再描画している。
- **修正指示**: 表示用の現在時刻更新を `Header`、フェーズ表示、カード、コンパクト表示などの末端コンポーネントへ移す。残秒数は既存どおり `startTime` と `seconds` から各表示で算出する。終了処理は最短の期限に対する one-shot timeout とし、実際に終了した時だけグローバル state を更新する。
- **注意点**: タイマー本体の `startTime`・停止・リセットの意味を変えない。複数タイマーがあるため、終了時には最新 state で再判定する。
- **再発防止**: 画面全体の state を一定周期で更新する `setInterval` を禁止し、時刻表示用フックは leaf component だけで使うレビュー項目を追加する。

### CB-023: コンポーネント契約の不一致

- **検出内容**: `App.tsx` が `Header`、`PhaseCard`、`PhaseProgressNav`、`QuickActionsModal`、`SyncTroubleshooter` に定義されていない props を渡している。一部の現行機能（フェーズ検索・設定）も Quick Actions から到達できない。
- **修正指示**: 各 props interface と呼び出し側を一致させる。廃止済み props は削除し、必要なフェーズ検索・設定は Quick Actions の正式な optional callback として定義して UI に接続する。
- **注意点**: props を `any` 化して通過させない。公開コンポーネントの props は単一の interface を唯一の契約にする。
- **再発防止**: `typecheck` を配備ゲートに入れ、コンポーネント改修時は呼び出し元を `rg` で全件確認する。

### CB-024: ドメイン型と実データの不一致

- **検出内容**: 台本ブロックの `image`、フェーズの旧 `title`、画像の `updatedAt`、シナリオの `rules`／`outline`、ショートカット定義、画像ごとの overlay 設定が型に反映されていない。
- **修正指示**: 実際に永続化・描画しているフィールドを `src/types.ts` に後方互換な optional field として定義し、生成箇所では必須データ（例: `updatedAt`）を補完する。旧データを読む正規化処理の型も実データと一致させる。
- **注意点**: 既存 Firestore ドキュメントには旧形式が残るため、必須化して既存読込を壊さない。新規保存では正規形を生成する。
- **再発防止**: Firestore に書くオブジェクトは共通型を明示し、スナップショットの round-trip テストに旧形式と新形式を含める。

### CB-025: Sync Studio の型欠落と更新副作用

- **検出内容**: `imageConfigs` の `overlayIntensity`、`imageFit: 'fill'`、`lapTexts` が同期型に欠ける。加えて `SyncWindowModal` は state updater 内から同期保存を呼ぶ実装になっている。
- **修正指示**: 既存ランタイム値を同期型へ追加する。state updater の中で外部書込みを行わず、次の設定を明示的に組み立ててから state 更新と同期保存を一度ずつ呼ぶ形へ段階的に整理する。
- **注意点**: 後半の副作用整理は Sync Studio のコア変更である。CB-029 と合わせ、所有者承認後にステージングで共有表示・画像設定・タイマー操作を検証する。
- **再発防止**: React state updater は純粋関数とし、Firestore 書込みはイベントハンドラまたは effect に限定する lint／レビュー規約を設ける。

### CB-026: Firebase Hosting の配備定義がない

- **検出内容**: `firebase.json`、プロジェクト別名、SPA rewrite、キャッシュ方針が未定義である。
- **修正指示**: `dist` を公開ディレクトリに指定し、全ルートを `index.html` に rewrite する Firebase Hosting 設定を追加する。rewrite 前のリクエストパスに header 規則が評価されるため、SPA の任意 URL は `**` で再検証し、`/assets/**` は後続規則で長期 immutable cache とする。AI Studio の Firebase プロジェクト ID を default alias に設定する。
- **注意点**: 初回 deploy はプロジェクト所有者の Firebase CLI ログインと対象プロジェクト確認が必要であり、ここでは実行しない。
- **再発防止**: Hosting preview channel で deep link とキャッシュ更新を確認してから production deploy する手順を README に固定する。

### CB-027: クライアントバンドルへの Gemini API キー注入

- **検出内容**: `vite.config.ts` の `define` が `GEMINI_API_KEY` をブラウザ用バンドルへ文字列埋込みする。現在のアプリコードは当該変数を参照していない。
- **修正指示**: `loadEnv` と API キーの `define` を削除する。将来 Gemini を使う場合は、秘密鍵をクライアントに置かず Firebase Functions / Cloud Run 等のサーバー側に限定する。
- **注意点**: Firebase Web config の API key は識別子であり同列の秘密鍵ではないが、GCP 側で referrer 制限と必要 API の制限を行う。
- **再発防止**: CI で bundle と設定ファイルを secret scanner にかけ、`GEMINI_API_KEY` や server key を `VITE_*`／`define` に入れない規約を設ける。

### CB-028: 依存パッケージの既知脆弱性

- **検出内容**: `npm audit` が 11 件（critical 2、high 7 を含む）を報告した。主に Vite と Firebase の推移的依存関係で、非破壊の audit fix 対象である。
- **修正指示**: `npm audit fix` を実施し、更新後に `npm run verify` と `npm audit` を再実行する。
- **注意点**: `--force` を使わない。更新結果が lockfile 以外を変えた場合は API 互換性を確認する。
- **再発防止**: 月次依存更新と、critical/high を配備ブロッカーとする監査基準を設ける。

### CB-029: 公開同期ドキュメントの PIN と推測可能なセッション ID

- **重要度**: P0 — 所有者承認済み、v1.09 で実施。
- **検出内容**: Firestore rule が公開 `get` を許し、共有 session ID が `uid_scenarioId` 形式で推測可能である。同期データには平文 PIN が含まれ、URLを知る第三者が PIN を読める設計になっていた。
- **修正内容**: `crypto.getRandomValues` により 256-bit の共有 capability ID を発行し、Timer は `timer~uid~capability`、Handout は `handout~uid~capability` の正規形式に統一した。Firestore は capability と document ID の一致だけを単一 `get` で許可し、`list`、旧 URL、平文 `pinCode` を拒否する。共有ペイロードから更新者 UID を削除し、キャラクターごとに独立した Handout capability を永続化する。
- **独立再評価と妥当性**: 未認証プレイヤー画面を維持する以上、「URL を知る人が一つの共有ドキュメントを読む」能力型 URL は必要である。そのため public `get` を完全廃止するという初版指示は現行要件と両立しない。推測不可能な 256-bit capability、`list` 禁止、所有者のみの更新、PIN 非保存という現在の方針に訂正した。リンクを受け取った人は内容を閲覧できるため、リンク自体は秘密情報として扱う。
- **注意点**: 旧 QR／共有 URL は意図的に無効になる。`firestore.rules` と Hosting の新 build を同じリリースで配備してから、GM が Sync Studio／Handout を開いて新 URL を発行する。共有する画像 URL はプレイヤー表示に必要なため残る。画像自体に認可・失効・PIN が必要なら、次段階として Firebase Storage rules と短期 signed URL、または Cloud Functions を導入する。
- **再発防止**: セキュリティ設計レビューで「クライアントに届く値はすべて閲覧可能」と扱う。Firebase Emulator の rules test に未認証の capability `get`、`list` 拒否、旧 URL 拒否、第三者の create/update/delete 拒否、`pinCode` 拒否を追加する。URL 誤送信時の capability 再発行手順も運用手順に固定する。

### CB-030: 本番 bundle の大きさ

- **検出内容**: main chunk は minify 後約 1.83 MB（gzip 約 507 kB）で、Vite が chunk size warning を報告する。Firestore の static/dynamic import 混在警告もある。
- **修正指示**: まず route/modal 単位の `React.lazy` を検討し、FireStore import を一貫した方式にする。変更後は通信条件を絞った端末で初期表示を計測する。
- **注意点**: 同期画面は初期操作に必要なため、無計画に遅延読込しない。機能単位で fallback と再接続を検証する。
- **再発防止**: CI に bundle size 予算を設け、閾値を超えた変更では根拠をレビューに記録する。

## 実施順序

1. CB-020、CB-028 で依存状態を安定化する。
2. CB-024、CB-023、CB-021 で typecheck を通す。
3. CB-022 を適用し、タイマー表示を末端更新へ移す。
4. CB-026、CB-027 を適用して Hosting 配備基盤を整える。
5. lint、typecheck、test、build、audit を再実行する。
6. CB-029 は所有者承認後に実施済み。Firestore Emulator の rules test と Hosting preview channel の実機受入を配備前に行う。

## 配備前の必須確認

- `npm ci`
- `npm run verify`
- `npm audit`
- Firebase Hosting preview channel で直接 URL、リロード、タイマー共有、画像共有を確認
- Firestore rules と API key の GCP 制限を所有者が確認

## 独立再評価で追加・修正した項目

### CB-031: README が不要な Gemini API key を必須としていた

- **検出内容**: 実装から API key のクライアント注入を削除した後も、README が `.env.local` に `GEMINI_API_KEY` を設定するよう案内していた。
- **修正指示**: ローカル実行手順を `npm ci` と `npm run dev` に更新し、Gemini の秘密鍵をクライアント設定へ置かないことを明記する。Firebase Hosting の preview channel と production deploy の手順も追加する。
- **注意点**: README の手順は、新規参加者が最初に信頼する運用情報である。実装と違う認証・鍵の説明は、不要な鍵漏えいと初回起動失敗を招く。
- **再発防止**: 配備前に「README のコマンドをクリーン環境で実行する」確認をリリースチェックリストへ追加する。

### CB-032: Hosting 上の実ブラウザ受入試験が未完了

- **検出内容**: 本作業環境の内蔵ブラウザはローカル Vite サーバーとネットワーク名前空間を共有せず、実際の画面操作試験を自動化できなかった。lint、型検査、57 ユニットテスト、本番ビルド、lockfile からの `npm ci` は通過しているが、Firebase Hosting 上の画面確認は未実施である。
- **修正指示**: 初回は preview channel に配備し、(a) `/` と任意の深い URL を直接開いて再読み込み、(b) Google ログイン、(c) タイマー開始・停止・00:00、(d) Sync Studio の画像・オーバーレイ・QR 共有、(e) 新版配備後の assets 更新、を実機で確認する。
- **注意点**: ローカル build 成功は Firestore rules、OAuth 承認済みドメイン、Hosting CDN header の実環境挙動を保証しない。
- **再発防止**: preview channel を本番前の必須段階とし、上記 5 項目を手動受入試験記録に残す。

### CB-033: 画面上の現在バージョン表示と変更履歴が乖離

- **検出内容**: `Spec-History.md` の最新は v1.07 だった一方、ヘッダーとヘルプ画面は v0.96md を現在版として表示していた。
- **修正指示**: リリースの更新ログを UI 用 `UPDATE_LOGS` に追加し、ヘッダー・ヘルプ・Web マニュアルの現在バージョン表示を同じ版へ統一する。過去版の説明は履歴として保持する。
- **実施内容**: v1.09 の更新ログ、ヘッダー、ヘルプ、Webマニュアル、および新規／リセット用の初期シナリオをv1.09へ更新した。既に利用者が保存したシナリオ本文は、意図しない書換えを避けるため自動変更しない。
- **注意点**: バージョンを表示だけ更新しても、更新内容が利用者に伝わなければ配備判定に使えない。既存シナリオの台本文は利用者データであり、一括置換しない。
- **再発防止**: リリースごとに `Spec-History.md`、`UPDATE_LOGS`、ヘッダーの `currentVersion`、マニュアルの 4 箇所を同じ変更に含める。

### CB-034: グローバルショートカットの実行経路を自動検証できない

- **検出内容**: `useGlobalShortcuts` は多数の分岐と React callback を一つの effect に持ち、Sync Studio、同期メディア、BGM/SE、タイマー、フェーズ、編集操作の回帰をユニットテストで確認できなかった。設定キーやキーボード配列を追加した際に、見た目では分からない未接続が起きる余地がある。
- **修正内容**: DOM 依存部分を `isEditableShortcutTarget` に限定し、通常操作を `dispatchGlobalShortcut`、編集操作を `dispatchEditorShortcut` として切り出した。`useGlobalShortcuts.test.ts` で Sync Studio 開閉、メディア番号・前後、BGM、SE、全音声停止、タイマー開始停止・リセット、フェーズ、クイック操作、検索、編集切替、Undo/Redo/保存、利用者定義キーを callback 到達で検証する。
- **注意点**: テキスト入力中はグローバルショートカットを発火させない。ブラウザ／OS が先に予約するキーや、実際の Web Audio の再生可否は配備先ブラウザでの手動確認も必要である。
- **再発防止**: 新しいショートカットを追加・変更する pull request には dispatcher のテストケースを必須にする。`KeyboardShortcuts` 型、Preferences、Shortcuts Guide、Web マニュアル、dispatcher を同じ変更で見直す。

### CB-035: Handout の在席通知だけで既存資料を置換し得る

- **検出内容**: `SyncService.updateHandout` は `playerPresentAt` だけを更新する場合に Firestore の merge を無効化していた。この経路が呼ばれると、既存の本文・履歴・キャラクター情報が消えるおそれがある。
- **修正内容**: Handout の全書込みを `{ merge: true }` に統一し、在席通知・本文・履歴のいずれも既存フィールドを保持する。secure Timer 書込み、旧 URL 拒否、在席通知単独更新を `SyncService.test.ts` で結合テスト化した。
- **注意点**: Firestore の `setDoc` は merge を指定しないと置換である。部分更新のAPIでは、更新対象以外を意図的に消す場合だけ置換を使う。
- **再発防止**: 新しい部分更新を追加する際は「既存フィールドが保持される」ケースをテストに含める。メッセージ送信・在席通知・設定更新を同時に行うケースは Emulator 受入試験にも追加する。

## 再評価の結論と実施状況

| ID | 再評価結果 | 実施状況 |
| --- | --- | --- |
| CB-020 | 妥当 | lockfile を再生成し、`npm ci` を完走 |
| CB-021 | 妥当 | `typecheck` と `verify` を追加 |
| CB-022 | 妥当 | root の毎秒更新を除去し、末端表示と one-shot timeout に移行 |
| CB-023 / CB-024 | 妥当 | props 契約・旧データ互換型・テスト fixture を修正 |
| CB-025 | 妥当 | 同期型の欠落は修正。state updater 内の外部書込み整理はコア変更のため保留 |
| CB-026 | header 評価順を踏まえて修正 | Hosting 設定、SPA rewrite、cache 方針、project alias を追加 |
| CB-027 | 妥当 | Gemini key の build 注入を削除 |
| CB-028 | 妥当 | 非破壊の依存更新後、`npm ci` の audit は 0 vulnerabilities |
| CB-029 | capability URL 方針へ訂正して妥当 | 明示承認後、v1.09 でランダム ID、PIN 削除、旧 URL／list 拒否、所有者限定更新を実装。preview と Emulator rules test は配備前条件 |
| CB-030 | 妥当 | 507 kB gzip の main chunk 警告は残る。遅延読込を別変更として扱う |
| CB-031 | 追加 | README を実装と配備手順に合わせて修正 |
| CB-032 | 追加 | preview channel の実機受入試験が配備条件 |
| CB-033 | 追加 | 画面・ヘルプ・マニュアルの現在バージョンを v1.09 に統一 |
| CB-034 | 妥当 | dispatcher 化と 57 件の回帰テストで、同期・音源・タイマーを含む全ショートカット経路を確認 |
| CB-035 | 妥当 | 在席通知の置換バグを merge 更新へ修正し、該当サービス結合テストを通過 |

## 最終検証結果

- `npm ci`: 成功（監査 0 vulnerabilities）
- `npm run verify`: 成功（lint、typecheck、57 tests、production build）
- 追加エッジテスト: `npm run test` は60 tests、修正箇所限定の `npm run test -- src/services/SyncService.test.ts` は3 testsで成功
- 残存する build 警告: Firestore の static/dynamic import 混在、および gzip 507 kB の main chunk
- **本番 deploy の阻止条件**: v1.09 の Firestore rules を Hosting と同じリリースで deploy し、preview channel 上で新規 Timer／Handout URL、タイマー、音源、ショートカットを実機受入すること
